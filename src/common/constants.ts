export const PAGE_SIZE = 10;
export enum METHODS {
  POST = "POST",
  GET = "GET",
  PATCH = "PATCH",
}

export enum REFERENCES {
  USER = "User",
  ROLE = "Role",
}

export enum POPULATE {
  USER = "user",
  ROLE = "role",
}

export enum ERoles {
  CLIENT = "CLIENT",
  PROFESSIONAL = "PROFESSIONAL",
  ADMIN = "ADMIN",
}
